export class Utils {
    randomRange(min, max){
        return Math.floor(Math.random() * (max - min) + min)
    }

    randomNumber(upper) {
        return Math.floor(Math.random() * upper)
    }

    randomRGBA() {
        // TODO
    }
}